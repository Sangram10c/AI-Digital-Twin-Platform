/**
 * GitHub OAuth Strategy (Placeholder)
 */
import { Injectable } from '@nestjs/common';

@Injectable()
export class GithubStrategy {
  // Will extend PassportStrategy(Strategy) from passport-github2
}
