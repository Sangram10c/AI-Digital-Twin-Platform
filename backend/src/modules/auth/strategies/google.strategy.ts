/**
 * Google OAuth Strategy (Placeholder)
 */
import { Injectable } from '@nestjs/common';

@Injectable()
export class GoogleStrategy {
  // Will extend PassportStrategy(Strategy) from passport-google-oauth20
}
