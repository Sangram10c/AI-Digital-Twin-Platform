# Project Rules

- **Git Commands Restriction**: Do not run `git commit`, `git add`, or `git push` commands. Only modify and update files in the workspace. The user will handle committing and pushing code themselves.
