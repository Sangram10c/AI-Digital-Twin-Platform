# Nginx Configuration

Nginx reverse proxy and load balancer configurations for the AI Digital Twin Platform.

## Status

Not yet configured. This directory will contain:

- `nginx.conf` - Main Nginx configuration
- `conf.d/` - Site-specific configurations
- SSL/TLS certificate management
